<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xnjr.mall.dao.IHzbMgiftDAO">
	<resultMap id="hzbMgift_DB_Result" type="hzbMgift">
		<result column="code" property="code" />
		<result column="adv_title" property="advTitle" />
		<result column="owner" property="owner" />
		<result column="owner_currency" property="ownerCurrency" />
		<result column="owner_amount" property="ownerAmount" />
		<result column="receive_currency" property="receiveCurrency" />
		<result column="receive_amount" property="receiveAmount" />
		<result column="receiver" property="receiver" />
		<result column="receive_datetime" property="receiveDatetime" />
		<result column="status" property="status" />
		<result column="create_datetime" property="createDatetime" />
		<result column="remark" property="remark" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="code != null and code != '' ">
				AND t.code = #{code}
			</if>
			<if test="owner != null and owner != '' ">
				AND t.owner = #{owner}
			</if>
			<if test="receiver != null and receiver != '' ">
				AND t.receiver = #{receiver}
			</if>
			<if test="status != null and status != '' ">
				AND t.status = #{status}
			</if>
		</trim>
	</sql>
	<select id="select_hzbMgift" parameterType="hzbMgift" resultMap="hzbMgift_DB_Result">
		SELECT
		t.code
		,t.adv_title
		,t.owner
		,t.owner_currency
		,t.owner_amount
		,t.receive_currency
		,t.receive_amount
		,t.receiver
		,t.receive_datetime
		,t.status
		,t.create_datetime
		,t.remark
		FROM
		tact_hzb_mgift t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>

	<select id="select_hzbMgift_count" parameterType="hzbMgift"
		resultType="java.lang.Long">
		SELECT count(1) FROM tact_hzb_mgift t
		<include refid="where_condition" />
	</select>

	<insert id="insert_hzbMgift" parameterType="hzbMgift">
		INSERT INTO
		tact_hzb_mgift(
		code
		,adv_title
		,owner
		,owner_currency
		,owner_amount
		,receive_currency
		,receive_amount
		,receiver
		,receive_datetime
		,status
		,create_datetime
		,remark
		)
		VALUES(
		#{code}
		,#{advTitle}
		,#{owner}
		,#{ownerCurrency}
		,#{ownerAmount}
		,#{receiveCurrency}
		,#{receiveAmount}
		,#{receiver}
		,#{receiveDatetime}
		,#{status}
		,#{createDatetime}
		,#{remark}
		)
	</insert>

	<update id="update_hzbMgift_status" parameterType="hzbMgift">
		UPDATE tact_hzb_mgift
		SET
		status = #{status}
		,remark = #{remark}
		WHERE code = #{code}
	</update>
	
	<update id="update_hzbMgift_receiver" parameterType="hzbMgift">
		UPDATE tact_hzb_mgift
		SET
		status = #{status}
		,receiver = #{receiver}
		,receive_datetime = #{receiveDatetime}
		,remark = #{remark}
		WHERE code = #{code}
	</update>
</mapper>
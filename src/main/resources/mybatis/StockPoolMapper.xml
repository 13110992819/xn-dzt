<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xnjr.mall.dao.IStockPoolDAO">
	<resultMap id="stockPool_DB_Result" type="stockPool">
		<result column="code" property="code" />
		<result column="name" property="name" />
		<result column="type" property="type" />
		<result column="amount" property="amount" />
		<result column="used_amount" property="usedAmount" />
		<result column="company_code" property="companyCode" />
		<result column="system_code" property="systemCode" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="code != null and code != '' ">
				AND t.code = #{code}
			</if>
			<if test="name != null and name != '' ">
				AND t.name = #{name}
			</if>
			<if test="type != null and type != '' ">
				AND t.type = #{type}
			</if>
			<if test="companyCode != null and companyCode != '' ">
				AND t.company_code = #{companyCode}
			</if>
			<if test="systemCode != null and systemCode != '' ">
				AND t.system_code = #{systemCode}
			</if>
		</trim>
	</sql>
	<select id="select_stockPool" parameterType="stockPool" resultMap="stockPool_DB_Result">
		SELECT
		t.code
		,t.name
		,t.type
		,t.amount
		,t.used_amount
		,t.company_code
		,t.system_code
		FROM
		to2o_stock_pool t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>

	<select id="select_stockPool_count" parameterType="stockPool"
		resultType="java.lang.Long">
		SELECT count(1) FROM to2o_stock_pool t
		<include refid="where_condition" />
	</select>

	<insert id="insert_stockPool" parameterType="stockPool">
		INSERT INTO
		to2o_stock_pool(
		code
		,name
		,type
		,amount
		,used_amount
		,company_code
		,system_code
		)
		VALUES(
		#{code}
		,#{name}
		,#{type}
		,#{amount}
		,#{usedAmount}
		,#{companyCode}
		,#{systemCode}
		)
	</insert>

	<update id="update_amount" parameterType="stockPool">
		UPDATE to2o_stock_pool
		SET
		amount = #{amount}
		WHERE code = #{code}
	</update>
	
	<update id="update_usedAmount" parameterType="stockPool">
		UPDATE to2o_stock_pool
		SET
		used_Amount = #{usedAmount}
		WHERE code = #{code}
	</update>
</mapper>